export enum Permissions {
  ADMIN,
  READ_ONLY,
}
// shorter version instead of variables
export enum LoyaltyUser {
    GOLD_USER = "GOLD-USER",
    SILVER_USER = "SILVER-USER",
    BRONZE_USER = "BRONZE-USER" 
}
